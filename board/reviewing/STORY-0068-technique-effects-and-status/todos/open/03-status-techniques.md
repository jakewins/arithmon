# Todo: Add two status-applying techniques

## What

Add to `src/game/data/techniques.ts`:

- `poisonSting`: venom element, melee, low power damage + ~30% applyStatus poisoned to opponent.
- `lullaby`: normal element, ranged, no damage, ~75% applyStatus sleep to opponent. dpCost = 2.

Assign at least one of these to a level-up moveset on an existing monster so it can be triggered organically in combat.
