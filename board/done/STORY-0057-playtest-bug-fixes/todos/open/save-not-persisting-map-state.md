# Save doesn't persist current map state

When choosing "save" in the menu and then refreshing the browser, all progress in the current map area is lost. It appears the game only saves when moving between maps. Choosing "save" should immediately persist all current state.
