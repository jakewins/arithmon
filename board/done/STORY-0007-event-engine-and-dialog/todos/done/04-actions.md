# Task 4: Action registry + first actions

Implemented actions:
- `dialog` — multi-frame: typewriter text overlay, ▼ prompt, dismiss on INTERACT
- `set_variable` — single-frame: parses key:value, stores in GameVariables
- `create_npc` — single-frame: adds sprite at tile position using player spritesheet
