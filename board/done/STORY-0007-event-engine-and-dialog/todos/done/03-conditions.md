# Task 3: Condition registry + first conditions

Implemented `src/game/event/registry.ts` and conditions:
- `char_facing_tile` — checks if player faces the event's tile zone
- `button_pressed` — checks INTERACT keypress this frame
- `variable_set` — checks GameVariables for key:value match
