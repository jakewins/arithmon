# Scaffold Phaser + Vite + TypeScript project

Clone or degit the official `phaserjs/template-vite-ts` template into the repo root
(alongside `board/`, `scripts/`, etc). Run `npm install` and verify `npm run dev`
launches the default Phaser splash screen in the browser.
