# Set up Vitest with a smoke test

Install Vitest (pairs naturally with Vite). Write a bare-bones test (e.g. `expect(1).toBe(1)`)
to confirm the test runner and TS compilation work. Add an `npm test` script. Verify it passes.
