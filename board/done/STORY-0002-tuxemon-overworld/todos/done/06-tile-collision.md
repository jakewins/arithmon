# Add basic tile collision

- Mark solid tiles in the tilemap (via a collision layer or tile properties)
- Enable Phaser Arcade Physics on the player
- Set collision between the player and the solid tile layer
- Set world bounds to the map size so the player can't walk off the edge
- Verify the player stops when hitting a solid tile
