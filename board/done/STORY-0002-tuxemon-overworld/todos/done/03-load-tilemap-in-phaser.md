# Load and render the Tiled map in Phaser

Create an `OverworldScene` that:

- Preloads the tilemap JSON and tileset images
- Creates the tilemap and tile layers via Phaser's Tilemaps API
- Renders the map in the browser

Replace HelloScene as the startup scene. At this point it's just a static map
with no character or interaction.
